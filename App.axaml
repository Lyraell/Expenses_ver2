<Application
    x:Class="Expenses.App"
    xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:Expenses.ViewModels"
             xmlns:views="clr-namespace:Expenses.Views"
             RequestedThemeVariant="Default">
	
	<Application.Resources>
		<ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--  Optional: put theme or other shared resources here  -->
            </ResourceDictionary.MergedDictionaries>

            <!--  ✅ These templates directly inside ResourceDictionary  -->
			<DataTemplate DataType="{x:Type viewmodels:AddExpenseViewModel}">
				<views:AddExpenseView />
			</DataTemplate>
			<DataTemplate DataType="{x:Type viewmodels:ExpenseListViewModel}">
				<views:ExpenseListView />
			</DataTemplate>
		</ResourceDictionary>
	</Application.Resources>

	<Application.Styles>
		<FluentTheme />
	</Application.Styles>
</Application>
